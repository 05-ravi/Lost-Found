// Post item placeholder
import 'package:flutter/material.dart';

class PostItemScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('Post Item')));
}
